// Physics — all values are per-second (not per-frame)
export const GRAVITY = -8.0;
export const JUMP_VELOCITY = 5.0;
export const MAX_FALL_SPEED = 5.0;
export const SCROLL_SPEED = 3.0;

// Obstacles
export const OBSTACLE_INTERVAL = 8.0;
export const OBSTACLE_START_DISTANCE = 12.0;

// World bounds
export const CEILING_Y = 15.0;
export const GROUND_Y = 0.5;

// Rendering
export const FOG_DENSITY = 0.0005;

// Game
export const DEATH_DELAY_FRAMES = 30;
export const DEATH_KNOCKBACK_VELOCITY = -1.5;
